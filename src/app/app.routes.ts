import { Routes } from '@angular/router';
import { FontWeightsComponent } from './routes/font-weights/font-weights.component';

export const routes: Routes = [
   {
      path: '',
      component: FontWeightsComponent
   }
];
